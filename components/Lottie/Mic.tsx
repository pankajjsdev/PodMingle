import React from "react";
import Lottie from "lottie-react";
import mic from "@/public/images/lottie/mic.json";

const Mic = () => <Lottie animationData={mic} loop={true} className="w-40 h-40" />;

export default Mic;